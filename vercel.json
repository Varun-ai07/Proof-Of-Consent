{
  "version": 2,
  "outputDirectory": "frontend",
  "builds": [
    {
      "src": "backend/src/server.js",
      "use": "@vercel/node",
      "config": { "maxDuration": 30 }
    }
  ],
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" }
  ]
}
